name: C/C++ CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build_linux:
    runs-on: ubuntu-latest
    steps:
    - name: Install dependencies
      run: |
        sudo apt-get -qq update
        sudo apt-get install -y -qq lmdb mecab
    - uses: actions/checkout@v4
    - name: make
      run: make
